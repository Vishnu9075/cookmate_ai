[project]
name = "cookmate-mvp"
version = "0.1.0"
description = "CookMate AI MVP - compile recipe + session engine"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.110",
  "uvicorn[standard]>=0.27",
  "pydantic>=2.6",
  "pydantic-settings>=2.2",
]

[tool.uvicorn]
factory = false
